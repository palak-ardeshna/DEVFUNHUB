export const codeSnippets = [
  "const calculateSum = (a, b) => { return a + b; };",
  "useEffect(() => { console.log('Mounted!'); }, []);",
  "const [data, setData] = useState(null);",
  "async function fetchData(url) { const res = await fetch(url); return res.json(); }",
  "class Developer extends Human { constructor(name) { super(name); this.coffee = true; } }",
  "export default function App() { return <div className='p-4'>Hello DevFun Hub!</div>; }",
  "git commit -m 'Fixed Heisenbug' && git push origin main",
  "npm install @tailwindcss/vite postcss autoprefixer",
  "const filtered = items.filter(item => item.active).map(i => i.id);",
  "while(coding) { if(bug) { cry(); debug(); } else { coffee++; } }"
];
